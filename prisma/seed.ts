// Placeholder seed script. Real seed data is added in Phase 2 once the catalog schema exists.
async function main() {
  console.log("No seed data yet — schema and seed data land in Phase 2.");
}

main();
